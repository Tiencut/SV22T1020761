@{
    Layout = null;
}
@model SV22T1020761.Models.Partner.Customer

<div class="modal-header">
    <h5 class="modal-title">@(Model?.CustomerID > 0 ? "Chỉnh sửa khách hàng" : "Thêm mới khách hàng")</h5>
    <button type="button" class="btn-close" data-bs-dismiss="modal" aria-label="Close"></button>
</div>

<form id="formCustomer" method="post" action="@(Model?.CustomerID > 0 ? Url.Action("Edit", "Customer") : Url.Action("Create", "Customer"))">
    @Html.AntiForgeryToken()
    <input type="hidden" asp-for="CustomerID" />
    
    <div class="modal-body">
        <div asp-validation-summary="ModelOnly" class="alert alert-danger" role="alert"></div>
        
        <div class="mb-3">
            <label class="form-label">Tên khách hàng <span class="text-danger">*</span></label>
            <div class="input-group">
                <span class="input-group-text"><i class="bi bi-shop"></i></span>
                <input asp-for="CustomerName" class="form-control" placeholder="Nhập tên khách hàng" required />
            </div>
            <span asp-validation-for="CustomerName" class="text-danger small d-block mt-1"></span>
        </div>
        
        <div class="mb-3">
            <label class="form-label">Tên giao dịch <span class="text-danger">*</span></label>
            <div class="input-group">
                <span class="input-group-text"><i class="bi bi-person-fill"></i></span>
                <input asp-for="ContactName" class="form-control" placeholder="Nhập tên liên hệ" required />
            </div>
            <span asp-validation-for="ContactName" class="text-danger small d-block mt-1"></span>
        </div>
        
        <div class="mb-3">
            <label class="form-label">Điện thoại</label>
            <div class="input-group">
                <span class="input-group-text"><i class="bi bi-telephone-fill"></i></span>
                <input asp-for="Phone" type="tel" class="form-control" placeholder="0123456789" />
            </div>
            <span asp-validation-for="Phone" class="text-danger small d-block mt-1"></span>
        </div>
        
        <div class="mb-3">
            <label class="form-label">Email</label>
            <div class="input-group">
                <span class="input-group-text"><i class="bi bi-envelope-fill"></i></span>
                <input asp-for="Email" type="email" class="form-control" placeholder="example@domain.com" />
            </div>
            <span asp-validation-for="Email" class="text-danger small d-block mt-1"></span>
        </div>
        
        <div class="mb-3">
            <label class="form-label">Địa chỉ</label>
            <div class="input-group">
                <span class="input-group-text"><i class="bi bi-geo-alt-fill"></i></span>
                <input asp-for="Address" class="form-control" placeholder="Nhập địa chỉ" />
            </div>
            <span asp-validation-for="Address" class="text-danger small d-block mt-1"></span>
        </div>
    </div>
    
    <div class="modal-footer">
        <button type="button" class="btn btn-outline-secondary" data-bs-dismiss="modal">
            <i class="bi bi-x-circle"></i> Hủy
        </button>
        <button type="submit" class="btn btn-primary">
            <i class="bi bi-save"></i> Lưu
        </button>
    </div>
</form>
<script>
    document.addEventListener('DOMContentLoaded', function () {
        var form = document.getElementById('formCustomer');
        form.addEventListener('submit', async function (e) {
            e.preventDefault();
            if (!$(form).valid()) return;
            var url = form.action;
            var formData = new FormData(form);
            try {
                var res = await fetch(url, {
                    method: 'POST',
                    headers: { 'X-Requested-With': 'XMLHttpRequest' },
                    body: formData
                });
                if (res.ok) {
                    var html = await res.text();
                    document.getElementById('customerTableContainer').innerHTML = html;
                    var modalEl = document.getElementById('dialogModal');
                    var modal = bootstrap.Modal.getInstance(modalEl) || new bootstrap.Modal(modalEl);
                    modal.hide();
                } else {
                    var html = await res.text();
                    document.querySelector('#dialogModal .modal-content').innerHTML = html;
                }
            } catch (err) {
                showToast('L?i khi Lưu: ' + err.message, 'error');
            }
        });
    });
</script>