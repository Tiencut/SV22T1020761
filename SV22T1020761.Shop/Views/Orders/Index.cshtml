@model SV22T1020761.Models.Common.PagedResult<SV22T1020761.Models.Sales.OrderViewInfo>
@using SV22T1020761.Models.Sales
@{
    ViewData["Title"] = "Đơn hàng của tôi";
}

<div class="auth-card">
    <h3>@ViewData["Title"]</h3>
    <div>
        <table class="table">
            <thead>
                <tr><th>Mã đơn hàng</th><th>Thời gian</th><th>Khách hàng</th><th>Trạng thái</th><th>Tổng cộng</th><th></th></tr>
            </thead>
            <tbody>
                @foreach(var o in Model.DataItems)
                {
                    <tr>
                        <td>@o.OrderID</td>
                        <td>@o.OrderTime</td>
                        <td>@o.CustomerName</td>
                        <td>@(((OrderStatusEnum)o.Status).GetDescription())</td>
                        <td>@o.TotalAmount</td>
                        <td><a asp-controller="Orders" asp-action="Details" asp-route-id="@o.OrderID" class="btn btn-sm btn-primary">Chi ti?t</a></td>
                    </tr>
                }
            </tbody>
        </table>
    </div>
</div>

@section Scripts {
}
